name: Beta Release

on:
  push:
    branches: [beta]

jobs:
  release:
    uses: Conduction/.github/.github/workflows/release-beta.yml@main
    with:
      app-name: zaakafhandelapp
    secrets: inherit
